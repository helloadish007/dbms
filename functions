CREATE OR REPLACE FUNCTION inc(n INT)
RETURNS INT AS 
$BODY$
BEGIN
	RETURN n+1; 
END;
$BODY$
LANGUAGE plpgsql;   

-------------------------
CREATE OR REPLACE FUNCTION fib(n1 INT,n2 INT)
RETURNS INT AS 
$BODY$
BEGIN
	RETURN n1+n2; 
END;
$BODY$
LANGUAGE plpgsql;   
